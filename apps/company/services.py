"""
Company service layer.

All business logic for company management lives here.
Views MUST delegate to these functions — no business logic in views.
"""

import logging

logger = logging.getLogger(__name__)

# Add service functions here as the module is developed.
# Example:
# def create_company(*, name, address, ...):
#     ...
